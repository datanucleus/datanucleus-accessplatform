<project name="accessplatform" default="archive" basedir=".">
    <!-- Access local build properties -->
    <property file="build.properties"/>
    <typedef file="build_copyjar.xml"/>

    <target name="clean" description="clean">
        <delete dir="${basedir}/target"/>
    </target>

    <!-- Overall target to build all archives -->
    <target name="archive" description="build archives" depends="clean">
        <antcall target="arch_nodeps"/>
        <antcall target="arch_deps"/>
        <antcall target="arch_excel"/>
        <antcall target="arch_ldap"/>
        <antcall target="arch_hbase"/>
        <antcall target="arch_json"/>
        <antcall target="arch_mongodb"/>
        <antcall target="arch_neo4j"/>
        <antcall target="arch_neodatis"/>
        <antcall target="arch_odf"/>
        <antcall target="arch_rdbms"/>
        <antcall target="arch_xml"/>
    </target>

    <target name="arch_rdbms" description="Build RDBMS archive">
        <echo>Generating DataNucleus AccessPlatform (RDBMS) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.rdbms.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
            group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-rdbms" version="${datanucleus-rdbms.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-geospatial" version="${datanucleus-geospatial.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${h2.group}" artifact="${h2.artifact}" version="${h2.version}"/>
        <copy file="${basedir}/licenses/h2.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.rdbms.name}.zip">
            <zipfileset prefix="${archive.rdbms.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_xml" description="Build XML archive">
        <echo>Generating DataNucleus AccessPlatform (XML) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.xml.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-xml" version="${datanucleus-xml.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jaxb.api.group}" artifact="${jaxb.api.artifact}" version="${jaxb.api.version}"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jaxb.impl.group}" artifact="${jaxb.impl.artifact}" version="${jaxb.impl.version}"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.xml.name}.zip">
            <zipfileset prefix="${archive.xml.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_excel" description="Build Excel archive">
        <echo>Generating DataNucleus AccessPlatform (Excel) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.excel.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-excel" version="${datanucleus-excel.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${poi.group}" artifact="${poi.artifact}" version="${poi.version}"/>
        <copy file="${basedir}/licenses/poi.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${poi.ooxml.group}" artifact="${poi.ooxml.artifact}" version="${poi.ooxml.version}"/>
        <copy file="${basedir}/licenses/poi.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.excel.name}.zip">
            <zipfileset prefix="${archive.excel.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_ldap" description="Build LDAP archive">
        <echo>Generating DataNucleus AccessPlatform (LDAP) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.ldap.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-ldap" version="${datanucleus-ldap.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${slf4j.group}" artifact="${slf4j.artifact}" version="${slf4j.version}"/>
        <copy file="${basedir}/licenses/slf4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${slf4j_log4j.group}" artifact="${slf4j_log4j.artifact}" version="${slf4j_log4j.version}"/>
        <copy file="${basedir}/licenses/slf4j.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.ldap.name}.zip">
            <zipfileset prefix="${archive.ldap.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_odf" description="Build ODF archive">
        <echo>Generating DataNucleus AccessPlatform (ODF) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.odf.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-odf" version="${datanucleus-odf.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${odfdom.group}" artifact="${odfdom.artifact}" version="${odfdom.version}"/>
        <copy file="${basedir}/licenses/odfdom.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${xerces.group}" artifact="${xerces.artifact}" version="${xerces.version}"/>
        <copy file="${basedir}/licenses/xerces.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.odf.name}.zip">
            <zipfileset prefix="${archive.odf.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_json" description="Build JSON archive">
        <echo>Generating DataNucleus AccessPlatform (JSON) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.json.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-json" version="${datanucleus-json.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.json.name}.zip">
            <zipfileset prefix="${archive.json.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_hbase" description="Build HBase archive">
        <echo>Generating DataNucleus AccessPlatform (HBase) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.hbase.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-hbase" version="${datanucleus-hbase.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${hbase.group}" artifact="${hbase.artifact}" version="${hbase.version}"/>
        <copy file="${basedir}/licenses/hbase.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${hadoop.core.group}" artifact="${hadoop.core.artifact}" version="${hadoop.core.version}"/>
        <copy file="${basedir}/licenses/hadoop.core.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.hbase.name}.zip">
            <zipfileset prefix="${archive.hbase.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_mongodb" description="Build MongoDB archive">
        <echo>Generating DataNucleus AccessPlatform (MongoDB) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.mongodb.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-mongodb" version="${datanucleus-mongodb.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${mongodb.group}" artifact="${mongodb.artifact}" version="${mongodb.version}"/>
        <copy file="${basedir}/licenses/mongodb.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.mongodb.name}.zip">
            <zipfileset prefix="${archive.mongodb.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_cassandra" description="Build Cassandra archive">
        <echo>Generating DataNucleus AccessPlatform (Cassandra) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.cassandra.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cassandra" version="${datanucleus-cassandra.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${cassandra.group}" artifact="${cassandra.artifact}" version="${cassandra.version}"/>
        <copy file="${basedir}/licenses/cassandra.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.cassandra.name}.zip">
            <zipfileset prefix="${archive.cassandra.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_neo4j" description="Build Neo4j archive">
        <echo>Generating DataNucleus AccessPlatform (Neo4j) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.neo4j.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-neo4j" version="${datanucleus-neo4j.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${neo4j.kernel.group}" artifact="${neo4j.kernel.artifact}" version="${neo4j.kernel.version}"/>
        <copy file="${basedir}/licenses/neo4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${neo4j.cypher.group}" artifact="${neo4j.cypher.artifact}" version="${neo4j.cypher.version}"/>
        <copy file="${basedir}/licenses/neo4j.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.neo4j.name}.zip">
            <zipfileset prefix="${archive.neo4j.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_neodatis" description="Build NeoDatis archive">
        <echo>Generating DataNucleus AccessPlatform (NeoDatis) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.neodatis.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-neodatis" version="${datanucleus-neodatis.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${neodatis.group}" artifact="${neodatis.artifact}" version="${neodatis.version}"/>
        <copy file="${basedir}/licenses/neodatis.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.neodatis.name}.zip">
            <zipfileset prefix="${archive.neodatis.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_deps" description="Build dependencies archive">
        <echo>Generating DataNucleus AccessPlatform (deps) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.deps.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-rdbms" version="${datanucleus-rdbms.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-neodatis" version="${datanucleus-neodatis.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-excel" version="${datanucleus-excel.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-xml" version="${datanucleus-xml.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-ldap" version="${datanucleus-ldap.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-json" version="${datanucleus-json.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-odf" version="${datanucleus-odf.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-hbase" version="${datanucleus-hbase.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-mongodb" version="${datanucleus-mongodb.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-rest" version="${datanucleus-api-rest.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-geospatial" version="${datanucleus-geospatial.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include all dependencies. -->
        <mkdir dir="${dist.dir}/deps"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jdo.group}" artifact="${jdo.artifact}" version="${jdo.version}"/>
        <copy file="${basedir}/licenses/jdo-api.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jpa.group}" artifact="${geronimo_jpa.artifact}" version="${geronimo_jpa.version}"/>
        <copy file="${basedir}/licenses/geronimo_jpa.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${geronimo_jta.group}" artifact="${geronimo_jta.artifact}" version="${geronimo_jta.version}"/>
        <copy file="${basedir}/licenses/geronimo_jta.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${ehcache.group}" artifact="${ehcache.artifact}" version="${ehcache.version}"/>
        <copy file="${basedir}/licenses/ehcache.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${log4j.group}" artifact="${log4j.artifact}" version="${log4j.version}"/>
        <copy file="${basedir}/licenses/log4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${jodatime.group}" artifact="${jodatime.artifact}" version="${jodatime.version}"/>
        <copy file="${basedir}/licenses/jodatime.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
        	group="${google.guava.group}" artifact="${google.guava.artifact}" version="${google.guava.version}"/>
        <copy file="${basedir}/licenses/google.guava.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${poi.group}" artifact="${poi.artifact}" version="${poi.version}"/>
        <copy file="${basedir}/licenses/poi.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${poi.ooxml.group}" artifact="${poi.ooxml.artifact}" version="${poi.ooxml.version}"/>
        <copy file="${basedir}/licenses/poi.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jaxb.api.group}" artifact="${jaxb.api.artifact}" version="${jaxb.api.version}"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${jaxb.impl.group}" artifact="${jaxb.impl.artifact}" version="${jaxb.impl.version}"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${h2.group}" artifact="${h2.artifact}" version="${h2.version}"/>
        <copy file="${basedir}/licenses/h2.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${neodatis.group}" artifact="${neodatis.artifact}" version="${neodatis.version}"/>
        <copy file="${basedir}/licenses/neodatis.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${slf4j.group}" artifact="${slf4j.artifact}" version="${slf4j.version}"/>
        <copy file="${basedir}/licenses/slf4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${slf4j_log4j.group}" artifact="${slf4j_log4j.artifact}" version="${slf4j_log4j.version}"/>
        <copy file="${basedir}/licenses/slf4j.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${odfdom.group}" artifact="${odfdom.artifact}" version="${odfdom.version}"/>
        <copy file="${basedir}/licenses/odfdom.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${xerces.group}" artifact="${xerces.artifact}" version="${xerces.version}"/>
        <copy file="${basedir}/licenses/xerces.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${hbase.group}" artifact="${hbase.artifact}" version="${hbase.version}"/>
        <copy file="${basedir}/licenses/hbase.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${hadoop.core.group}" artifact="${hadoop.core.artifact}" version="${hadoop.core.version}"/>
        <copy file="${basedir}/licenses/hadoop.core.license.txt" todir="${dist.dir}/deps"/>

        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/deps"
            group="${mongodb.group}" artifact="${mongodb.artifact}" version="${mongodb.version}"/>
        <copy file="${basedir}/licenses/mongodb.license.txt" todir="${dist.dir}/deps"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.deps.name}.zip">
            <zipfileset prefix="${archive.deps.name}" dir="${dist.dir}"/>
        </zip>
    </target>

    <target name="arch_nodeps" description="Build no-dependencies archive">
        <echo>Generating DataNucleus AccessPlatform (no-deps) archive</echo>
        <property name="dist.dir" value="${basedir}/target/${archive.nodeps.name}"/>
        <mkdir dir="${dist.dir}"/>
        <copy file="${basedir}/LICENSE.txt" todir="${dist.dir}"/>
        <copy file="${basedir}/README.txt" todir="${dist.dir}"/>

        <!-- Include all DataNucleus plugins. -->
        <mkdir dir="${dist.dir}/lib"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-core" version="${datanucleus-core.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jdo" version="${datanucleus-api-jdo.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-jpa" version="${datanucleus-api-jpa.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jpa-query" version="${datanucleus-jpa-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jdo-query" version="${datanucleus-jdo-query.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-rdbms" version="${datanucleus-rdbms.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-neodatis" version="${datanucleus-neodatis.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-excel" version="${datanucleus-excel.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-xml" version="${datanucleus-xml.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-ldap" version="${datanucleus-ldap.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-json" version="${datanucleus-json.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-odf" version="${datanucleus-odf.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-hbase" version="${datanucleus-hbase.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-mongodb" version="${datanucleus-mongodb.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-api-rest" version="${datanucleus-api-rest.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-cache" version="${datanucleus-cache.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-geospatial" version="${datanucleus-geospatial.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-jodatime" version="${datanucleus-jodatime.version}"/>
        <copy-m2-jar reporoot="${maven_repo}" dest="${dist.dir}/lib"
        	group="${datanucleus.group}" artifact="datanucleus-guava" version="${datanucleus-guava.version}"/>

        <!-- Include the tools -->
        <mkdir dir="${dist.dir}/tools"/>
        <copy todir="${dist.dir}/tools">
            <fileset dir="tools"/>
        </copy>

        <!-- Include the tutorials -->

        <!-- Create the zip -->
        <zip zipfile="${basedir}/target/${archive.nodeps.name}.zip">
            <zipfileset prefix="${archive.nodeps.name}" dir="${dist.dir}"/>
        </zip>
    </target>
</project>
